{
  "name": "festcountdown",
  "version": "1.0.0",
  "description": "A beautiful festival countdown website with SEO optimization",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp *.html dist/ && cp *.css dist/ && cp *.js dist/ && cp *.json dist/ && cp *.htaccess dist/ 2>/dev/null || true && cp -r images dist/ 2>/dev/null || true && cp -r api dist/",
    "dev": "node dev-server.js",
    "dev-simple": "python3 -m http.server 8000",
    "deploy": "npm run build && gh-pages -d dist",
    "vercel-build": "npm run build"
  },
  "keywords": [
    "countdown",
    "festival",
    "events",
    "diwali",
    "christmas",
    "new-year"
  ],
  "author": "Muhammed Salah",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
